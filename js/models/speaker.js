define(['jquery', 'underscore', 'backbone'], function($, _, Backbone){
           var Speaker = Backbone.Model.extend({
	                                           initialize : function() {
	                                           },
	                                           defaults : {
	                                           }
                                               });
           return Speaker;
       });
