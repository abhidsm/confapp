define(['jquery', 'underscore', 'backbone'], function($, _, Backbone){
           var Talk = Backbone.Model.extend({
	                                        initialize : function() {
	                                        },
	                                        defaults : {
	                                        }
                                            });
           return Talk;
       });
