define(['jquery', 'underscore', 'backbone'], function($, _, Backbone){
           var Conference = Backbone.Model.extend({
	                                              initialize : function() {
	                                              }
                                                  });
           return Conference;
       });
