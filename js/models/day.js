define(['jquery', 'underscore', 'backbone'], function($, _, Backbone){
           var Day = Backbone.Model.extend({
                                               initialize : function() {
                                               },
                                               defaults : {
                                               }
                                           });
           return Day;
       });
